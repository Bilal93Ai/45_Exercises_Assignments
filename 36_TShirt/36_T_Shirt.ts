
// 36. T-Shirt: Write a function called make_shirt() that accepts a size and the
// text of a message that should be printed on the shirt. The function should print
// a sentence summarizing the size of the shirt and the message printed on it.

// Call the function.

function make_shirt(size:any,text:any){
    console.log(`Creating a ${size} shirt size with a logo: ${text}`);
}
make_shirt('Small',`Nike`);
make_shirt('Medium',`Nike`);
make_shirt('Large',`Nike`);



// Practice


function make_shoes(size:any,text:any){
    console.log(`Making 100 ${size} shoes with a logo: ${text}`);
}
make_shoes('Small','Nike');
