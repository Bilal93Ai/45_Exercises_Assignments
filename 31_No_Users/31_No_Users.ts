// from ex 28 make sure list of user names is not empty...1 if list is empty print we need to find some users 
// 2 remove all usersnames frim arr and make sure correct message is been typed 

const userNames: string[] = ['Admin','BK ModTest','Zulu Kobra','lePepsi','Dumais'];

if(userNames.length === 0){
    console.log('we need to find some users');
}
else{
    let userNames = [];
    console.log('All usernames have been removed. ' + userNames.length);
}